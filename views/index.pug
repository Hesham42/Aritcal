extends layout
block content
    h1 #{title}
        ul
            each n, i in articales
                li= n.id
                li= n.title                                
                li= n.body